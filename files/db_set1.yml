pg_exporters:
  - name: db1
    exporter_port: 9187
    db_host: "{{ pg_host }}"
    db_port: "{{ pg_port }}"
    db_name: "{{ pg_db }}"
    db_user: "{{ pg_user }}"
    db_pass: "{{ pgexp_pass }}"
  - name: db2
    exporter_port: 9188
    db_host: "{{ pg_host }}"
    db_port: "{{ pg_port }}"
    db_name: "{{ pg_db }}"
    db_user: "{{ pg_user }}"
    db_pass: "{{ pgexp_pass }}"